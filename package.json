{
  "name": "module-federation",
  "private": true,
  "workspaces": {
    "packages": [
      "container",
      "remote"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
